<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dacode.collegeexperiment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CardOfBirthday" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ArithmeticCalculator" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".UserRegistration" />
        <activity android:name=".RegistrationConfermetion" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserDasboard" />
        <activity android:name=".MoviesActivity" />
        <activity android:name=".FoodItemsActivity"></activity>
    </application>

</manifest>